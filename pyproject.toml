[project]
name = "ritual"
version = "0.1.0"
description = "AI 编排训练计划 + 沉浸式训练模式(分步骤语音/动画/GIF)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "anthropic>=0.40",
    "openai>=1.30",
    "jinja2>=3.1",
    "pydantic>=2.5",
    "httpx>=0.27",
]

[tool.uv]
package = false